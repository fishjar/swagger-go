<% Object.entries(definitions)
  .filter(([_,model])=>model['x-isModel'])
  .forEach(([modelKey]) => { -%>
import <%- modelKey -%> from "./<%- modelKey.toLowerCase() -%>";
<% }) -%>

export default {
<% Object.entries(definitions)
  .filter(([_,model])=>model['x-isModel'])
  .forEach(([modelKey]) => { -%>
  <%- modelKey -%>,
<% }) -%>
};
